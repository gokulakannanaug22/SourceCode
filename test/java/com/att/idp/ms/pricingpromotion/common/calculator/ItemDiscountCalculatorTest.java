package com.att.idp.ms.pricingpromotion.common.calculator;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import com.att.idp.ms.pricingpromotion.common.PricingPromotionInputData;

@SuppressWarnings("deprecation")
public class ItemDiscountCalculatorTest {

	private ItemDiscountCalculator itemDiscountCalculator = new ItemDiscountCalculator();
	

	/*@Test
	public void calculateDiscountFlatAmountOffTest() {

		double resp = itemDiscountCalculator.calculateDiscountAmount(PricingPromotionInputData.getItemPromotionFlatAmountoffList());
		assertEquals(100.00, resp);
	}

	@Test
	public void calculateDiscountPercentOffTest() {

		double resp = itemDiscountCalculator.calculateDiscountAmount(PricingPromotionInputData.getItemPromotionPercentOfffList());
		assertEquals(100.00, resp);
	}
	
	@Test
	public void calculateDiscountFlatRateOffTest() {

		double resp = itemDiscountCalculator.calculateDiscountAmount(PricingPromotionInputData.getItemPromotionFlatRateList());
		assertEquals(100.00, resp);
	}

	@Test
	public void calculateFinalPriceWithFlatAmountOffPrice() {

		double resp = itemDiscountCalculator.calculateFinalPrice(PricingPromotionInputData.getItemPromotionFlatAmountoffList(), 12);
		assertEquals(100.00, resp);

	}

	@Test
	public void calculateFinalPriceWithFlatPercentOffPrice() {
		double resp = itemDiscountCalculator.calculateFinalPrice(PricingPromotionInputData.getItemPromotionPercentOfffList(), 12);
		assertEquals(100.00, resp);
	}
	
	@Test
	public void calculateFinalPriceWithFlatRatePrice() {
		double resp = itemDiscountCalculator.calculateFinalPrice(PricingPromotionInputData.getItemPromotionFlatRateList(), 12);
		assertEquals(100.00, resp);
	}
*/
	
}
